type Placement = "center" | "left" | "right";
interface ActionTooltipOptions {
    content: string;
    placement: Placement;
    pointer?: boolean;
}
export declare function tooltipAction<T extends HTMLElement>(node: T, options: string | ActionTooltipOptions): {
    destroy(): void;
};
export {};

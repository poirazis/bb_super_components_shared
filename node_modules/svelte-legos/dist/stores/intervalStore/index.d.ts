import { type Readable } from "svelte/store";
/**
 * Wrapper for `setInterval` with controls.
 *
 * @param cb
 * @param interval
 * @param options
 */
export declare function intervalStore(interval: number | Readable<number> | undefined): Readable<number>;

<script>import { timeoutStore } from "../..";
import DemoContainer from "../../shared/components/DemoContainer.svelte";
import PrimaryButton from "../../shared/components/PrimaryButton.svelte";
const { ready, start } = timeoutStore(2e3, { controls: true });
</script>

<DemoContainer>
	<p class="mb-4">Ready: {$ready}</p>
	<PrimaryButton disabled={!$ready} on:click={start}>Restart</PrimaryButton>
</DemoContainer>

import type { ConfigurableWindow, Fn } from "../../shared/utils/types";
/**
 * Wrapper for `requestAnimationFrame` with controls.
 *
 * @param cb
 * @param interval
 * @param options
 */
export declare function rafFnStore(fn: Fn, { window }?: ConfigurableWindow): {
    resume: () => void;
    pause: () => void;
};

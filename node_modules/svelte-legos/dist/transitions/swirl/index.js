import { linear } from "svelte/easing";
export function swirl(node, params = {}) {
    const { delay = 0, duration = 300, easing = linear } = params;
    return {
        delay,
        duration,
        easing,
        css: (t, u) => `transform: rotate(${t * -540}deg) scale(${t}); opacity: ${t}`,
    };
}
